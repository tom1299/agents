[project]
name = "agents-common"
version = "0.1.0"
description = "Shared helpers for workspace agents"

requires-python = ">=3.13"
dependencies = [
    "langchain==1.4.0",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"
