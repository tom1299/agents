[project]
name = "agent-tools"
version = "0.1.0"
description = "Shared tool agent"

requires-python = ">=3.13"
dependencies = ["langchain-core>=1.6.2"]

[project.scripts]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"
